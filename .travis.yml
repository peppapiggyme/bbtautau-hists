language: cpp

services:
  - docker

script:
  - docker pull atlasamglab/stats
  - docker run -d atlasamglab/stats /bin/bash
  - docker ps -a
  - docker run atlasamglab/stats /bin/bash
  - which root
  - pwd && ls
  - mkdir build
  - cd build
  - cmake ../ && make -j
